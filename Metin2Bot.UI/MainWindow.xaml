<Window x:Class="Metin2Bot.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Metin2Bot.UI.ViewModels"
        Title="Metin2Bot - Multi-Client Vision" Height="720" Width="1100"
        Background="#0F0F12" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#03DAC6"/>
            <Setter Property="Foreground" Value="#0F0F12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="14,8"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Opacity" Value="0.4"/>
                </Trigger>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#04EAD5"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="DangerButton" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#CF6679"/>
            <Setter Property="Foreground" Value="White"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E07080"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="Button" x:Key="SecondaryButton" BasedOn="{StaticResource PrimaryButton}">
            <Setter Property="Background" Value="#252836"/>
            <Setter Property="Foreground" Value="#E0E0E0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#33384A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#1A1C24"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#2C2F3D"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="6"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <Style TargetType="Border" x:Key="Card">
            <Setter Property="Background" Value="#16181F"/>
            <Setter Property="BorderBrush" Value="#22252F"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Padding" Value="14"/>
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0">
                <TextBlock Text="METIN2 VISION BOT" FontSize="24" FontWeight="ExtraBold">
                    <TextBlock.Foreground>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                            <GradientStop Color="#BB86FC" Offset="0"/>
                            <GradientStop Color="#03DAC6" Offset="1"/>
                        </LinearGradientBrush>
                    </TextBlock.Foreground>
                </TextBlock>
                <TextBlock Text="Multi-Client • Snip-to-Add • DEL ile Başlat/Durdur" FontSize="11" Foreground="#7F8294"/>
            </StackPanel>
            <Border Grid.Column="1" Style="{StaticResource Card}" Padding="14,8" VerticalAlignment="Top">
                <StackPanel Orientation="Horizontal">
                    <Ellipse Width="10" Height="10" VerticalAlignment="Center" Margin="0,0,8,0">
                        <Ellipse.Style>
                            <Style TargetType="Ellipse">
                                <Setter Property="Fill" Value="#7F8294"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsBotRunning}" Value="True">
                                        <Setter Property="Fill" Value="#03DAC6"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Ellipse.Style>
                    </Ellipse>
                    <TextBlock Text="{Binding BotStatus}" Foreground="#E0E0E0" VerticalAlignment="Center"/>
                </StackPanel>
            </Border>
        </Grid>

        <!-- Settings -->
        <Border Grid.Row="1" Style="{StaticResource Card}" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="1.4*"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Margin="0,0,6,0">
                    <TextBlock Text="Client Geçiş Süresi (ms)" Foreground="#9AA0B4" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding ClientSwitchDelayMs, UpdateSourceTrigger=PropertyChanged, FallbackValue=1000}"/>
                </StackPanel>
                <StackPanel Grid.Column="1" Margin="6,0">
                    <TextBlock Text="Eşleşme Eşiği (0.0 - 1.0)" Foreground="#9AA0B4" FontSize="11" Margin="0,0,0,4"/>
                    <TextBox Text="{Binding MatchThreshold, UpdateSourceTrigger=PropertyChanged, StringFormat=F2}"/>
                </StackPanel>
                <StackPanel Grid.Column="2" Margin="6,0,0,0" VerticalAlignment="Bottom">
                    <TextBlock Text="Tıklama Modu" Foreground="#9AA0B4" FontSize="11" Margin="0,0,0,4"/>
                    <CheckBox IsChecked="{Binding BringClientToFront}" Foreground="#E0E0E0" FontSize="12"
                              ToolTip="Multi-client'ta focus alternation bazı client'larda click drop edebilir; kapatınca kbunall referansı gibi saf background tıklar.">
                        <TextBlock Text="🪟 Her turda pencereyi öne getir"/>
                    </CheckBox>
                    <TextBlock Text="DEL: bot başlat/durdur (global)" Foreground="#7F8294" FontSize="10" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Clients & Products -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="320"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Sol: Client listesi -->
            <Border Grid.Column="0" Style="{StaticResource Card}" Margin="0,0,8,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <TextBlock Grid.Row="0" Text="CLIENT'LAR" FontSize="12" FontWeight="Bold" Foreground="#03DAC6" Margin="0,0,0,8"/>
                    <Grid Grid.Row="1" Margin="0,0,0,10">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="+ Client Ekle" Style="{StaticResource PrimaryButton}"
                                Command="{Binding AddClientCommand}" Margin="0,0,4,0"/>
                        <Button Grid.Column="1" Content="🗑" Style="{StaticResource DangerButton}"
                                Command="{Binding RemoveClientCommand}" Width="40" ToolTip="Seçili client'ı sil"/>
                    </Grid>
                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding Clients}"
                             SelectedItem="{Binding SelectedClient}"
                             Background="Transparent" BorderThickness="0">
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border x:Name="Border" Background="#1A1C24" CornerRadius="6" Padding="12,10" BorderBrush="#22252F" BorderThickness="1">
                                                <ContentPresenter/>
                                            </Border>
                                            <ControlTemplate.Triggers>
                                                <Trigger Property="IsSelected" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#1F2330"/>
                                                    <Setter TargetName="Border" Property="BorderBrush" Value="#03DAC6"/>
                                                </Trigger>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter TargetName="Border" Property="Background" Value="#1F2330"/>
                                                </Trigger>
                                            </ControlTemplate.Triggers>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel>
                                    <TextBlock Text="{Binding DisplayName}" FontWeight="SemiBold" Foreground="#E0E0E0"/>
                                    <TextBlock Foreground="#7F8294" FontSize="11">
                                        <Run Text="🪟 "/>
                                        <Run Text="{Binding WindowTitle}"/>
                                    </TextBlock>
                                    <TextBlock Foreground="#9AA0B4" FontSize="11">
                                        <Run Text="{Binding Products.Count, Mode=OneWay}"/>
                                        <Run Text=" ürün"/>
                                    </TextBlock>
                                </StackPanel>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>

            <!-- Sağ: Seçili client'ın ürünleri -->
            <Border Grid.Column="1" Style="{StaticResource Card}" Margin="8,0,0,0">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Grid Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="ÜRÜNLER" FontSize="12" FontWeight="Bold" Foreground="#03DAC6"/>
                            <TextBlock FontSize="11" Foreground="#9AA0B4">
                                <Run Text="Seçili: "/>
                                <Run Text="{Binding SelectedClient.DisplayName, FallbackValue=—}"/>
                            </TextBlock>
                        </StackPanel>
                        <Button Grid.Column="1" Content="+ Ürün Ekle (Snip)" Style="{StaticResource PrimaryButton}"
                                Command="{Binding AddProductCommand}"/>
                    </Grid>

                    <Border Grid.Row="1" Background="#1A1C24" CornerRadius="6" Padding="10,8" Margin="0,10,0,10"
                            Visibility="{Binding SelectedClient, Converter={x:Static vm:NullToVisibilityConverter.Inverse}, FallbackValue=Visible}">
                        <TextBlock Text="Önce sol panelden bir client seçin." Foreground="#9AA0B4" FontSize="11"/>
                    </Border>

                    <ListBox Grid.Row="2"
                             ItemsSource="{Binding SelectedClient.Products}"
                             Background="Transparent" BorderThickness="0"
                             ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Margin" Value="6"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="ListBoxItem">
                                            <Border Background="#1A1C24" CornerRadius="8" Padding="0" BorderBrush="#22252F" BorderThickness="1">
                                                <ContentPresenter/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                            </Style>
                        </ListBox.ItemContainerStyle>
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Width="180">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="120"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>
                                    <Border Grid.Row="0" Background="#0A0A0A" CornerRadius="8,8,0,0">
                                        <Image Source="{Binding Thumbnail}" Stretch="Uniform" Margin="6"/>
                                    </Border>
                                    <Grid Grid.Row="1" Margin="10,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Text="{Binding Name}" Foreground="#E0E0E0" TextTrimming="CharacterEllipsis" VerticalAlignment="Center"/>
                                        <Button Grid.Column="1" Content="🗑" Width="30" Height="26"
                                                Style="{StaticResource DangerButton}" Padding="0"
                                                Command="{Binding DataContext.RemoveProductCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                                CommandParameter="{Binding}"/>
                                    </Grid>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Border>
        </Grid>

        <!-- Logs -->
        <Border Grid.Row="3" Style="{StaticResource Card}" Margin="0,16,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="İŞLEM KAYITLARI" FontSize="12" FontWeight="Bold" Foreground="#03DAC6" Margin="0,0,0,8"/>
                <ListBox Grid.Row="1" ItemsSource="{Binding Logs}"
                         Background="Transparent" BorderThickness="0"
                         FontFamily="Consolas" FontSize="11" Foreground="#9AA0B4">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0,1"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>

        <!-- Bot Toggle (manuel buton — DEL hotkey'i ek olarak çalışır) -->
        <Grid Grid.Row="4" Margin="0,16,0,0">
            <Button Content="🟢 BOT AÇ / KAPA  (veya DEL tuşuna bas)"
                    Style="{StaticResource PrimaryButton}"
                    Command="{Binding ToggleBotCommand}"
                    Height="44" FontSize="14"/>
        </Grid>
    </Grid>
</Window>
