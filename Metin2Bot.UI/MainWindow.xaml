<Window x:Class="Metin2Bot.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Metin2Bot.UI"
        mc:Ignorable="d"
        Title="Metin2Bot - Premium Vision Bot" Height="600" Width="420"
        Background="#121212" Foreground="#E0E0E0"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,25">
            <TextBlock Text="METIN2 VISION BOT" FontSize="28" FontWeight="ExtraBold">
                <TextBlock.Foreground>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#BB86FC" Offset="0.0"/>
                        <GradientStop Color="#03DAC6" Offset="1.0"/>
                    </LinearGradientBrush>
                </TextBlock.Foreground>
            </TextBlock>
            <TextBlock Text="Premium Automation &amp; Image Processing" FontSize="12" Foreground="#7F7F7F" Margin="2,0,0,0"/>
        </StackPanel>

        <!-- Window Selection -->
        <StackPanel Grid.Row="1" Margin="0,0,0,20">
            <TextBlock Text="Hedef Oyun Penceresi" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#B0B0B0"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="50"/>
                </Grid.ColumnDefinitions>
                <ComboBox ItemsSource="{Binding Windows}" 
                          SelectedItem="{Binding SelectedWindow}"
                          DisplayMemberPath="Title"
                          Height="40" VerticalContentAlignment="Center" Padding="10,0"
                          Background="#1E1E1E" Foreground="White" BorderBrush="#333333">
                    <ComboBox.ItemContainerStyle>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="Background" Value="#1E1E1E"/>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#333333"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.ItemContainerStyle>
                </ComboBox>
                <Button Grid.Column="1" Command="{Binding RefreshWindowsCommand}"
                        Content="🔄" Margin="5,0,0,0" Background="#252525" Foreground="White" BorderBrush="#333333" FontSize="18"/>
            </Grid>
        </StackPanel>

        <!-- Controls -->
        <Grid Grid.Row="2" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="BOTU BAŞLAT" Command="{Binding StartBotCommand}"
                    Height="50" Margin="0,0,5,0" Background="#03DAC6" Foreground="#121212" FontWeight="Bold" FontSize="14" BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#04EAD5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
            <Button Grid.Column="1" Content="BOTU DURDUR" Command="{Binding StopBotCommand}"
                    Height="50" Margin="5,0,0,0" Background="#CF6679" Foreground="White" FontWeight="Bold" FontSize="14" BorderThickness="0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" CornerRadius="8">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#E07080"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Opacity" Value="0.5"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- Logs -->
        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <TextBlock Text="İşlem Kayıtları" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" Foreground="#B0B0B0"/>
            <Border Grid.Row="1" Background="#0A0A0A" BorderBrush="#252525" BorderThickness="1" CornerRadius="8">
                <ListBox ItemsSource="{Binding Logs}" 
                         Background="Transparent" BorderThickness="0" Padding="10"
                         Foreground="#A0A0A0" FontSize="12">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Focusable" Value="False"/>
                            <Setter Property="Margin" Value="0,2"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Border>
        </Grid>
    </Grid>
</Window>